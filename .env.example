# Puerto del servidor
PORT=4000

# PostgreSQL (Supabase, local, Render, etc.)
# Supabase: Project Settings → Database → Connection string (URI)
DATABASE_URL=postgresql://postgres:password@localhost:5432/spa_bar_db

# JWT
JWT_SECRET=cambia_este_secreto_por_uno_seguro_en_produccion
JWT_EXPIRES_IN=1d
JWT_REFRESH_SECRET=cambia_este_refresh_secreto_en_produccion
JWT_REFRESH_EXPIRES_IN=7d

# CORS - URL del frontend (Vercel en producción)
FRONTEND_URL=http://localhost:3000
